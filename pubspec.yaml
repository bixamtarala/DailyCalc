name: dailycalc
description: Offline-first Daily Money & Life Calculator for India.
publish_to: 'none'
version: 0.7.1+8
environment:
  sdk: '>=3.10.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  intl: ^0.20.2
  share_plus: ^13.3.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true
